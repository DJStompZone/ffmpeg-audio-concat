'use strict'


module.exports = async (opts) => {
  const {
    theme
  } = opts

  const frameWriter = null

  const ctx = {}

  ctx.setTransition = ({ name=null, resizeMode=null }) => {
  return null
    }

  ctx.flush = async () => {
    return null
  }

  ctx.dispose = async () => {
    return null
  }

  return ctx
}
